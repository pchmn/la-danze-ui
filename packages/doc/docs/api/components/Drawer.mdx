---
title: Drawer
hide_title: true
---

# `Drawer`

This component is a custom drawer. See [demo](https://pchmn.github.io/la-danze-ui/demo) for a live preview.

> Can't be used outside [`<DrawerTemplate>`](api/components/DrawerTemplate.mdx).

## Example

```jsx
import React from 'react';
import logo from './logo.svg';
import { 
  DrawerTemplate,
  DrawerContainer,
  MainContainer,
  Drawer
} from 'la-danze-ui';

function App() {
  return (
    <DrawerTemplate>
      <DrawerContainer>
        <Drawer logo={logo} title="Drawer">
          ...
        </Drawer>
      </DrawerContainer>

      <MainContainer>
        ...
      </MainContainer>
    </DrawerTemplate>
  );
}
```

## Props

import {PropsTable} from '/src/components/PropsTable';
export const componentProps = [
  {
    name: 'logo',
    type: 'string',
    required: true,
    description: 'The logo to use.'
  },
  {
    name: 'title',
    type: 'string',
    required: true,
    description: 'The title to use.'
  }
];

<PropsTable componentProps={componentProps} />