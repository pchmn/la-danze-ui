---
title: DrawerTemplate
hide_title: true
---

# `DrawerTemplate`

This component will create a template with a drawer. It must contain `DrawerContainer` and `MainContainer` as child.

> Can't be used outside [`<Theme>`](api/components/Theme.mdx).

## Example

```jsx
import { DrawerTemplate, DrawerContainer, MainContainer } from 'la-danze-ui';

function App() {
  return (
    // Must contain a DrawerContainer and a MainContainer
    <DrawerTemplate>
      <DrawerContainer>
        <div>drawer content</div>
      </DrawerContainer>
      <MainContainer>
        <div>main content</div>
      </MainContainer>
    </DrawerTemplate>
  );
}
```

## Props

import {PropsTable} from '../../../src/components/PropsTable';
export const drawerTemplateProps = [
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    description: <>The component tree (must contain <code>DrawerContainer</code> and <code>MainContainer</code>).</>
  }
];

<PropsTable componentProps={drawerTemplateProps} />


## `DrawerContainer`

> Can't be used outside`<DrawerTemplate>`.

### Props

export const drawerContainerProps = [
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    description: 'Drawer content'
  }
];

<PropsTable componentProps={drawerContainerProps} />


## `MainContainer`

> Can't be used outside`<DrawerTemplate>`.

### Props

export const mainContainerProps = [
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    description: 'Main content'
  }
];

<PropsTable componentProps={mainContainerProps} />
